{
  "name": "browser-pilot",
  "description": "A AI-based browser extension.",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "dev": "pnpm --filter extension dev",
    "zip": "pnpm --filter extension zip"
  }
}
